services:
  open-workout-app:
    build: .
    # Or, once published: image: ghcr.io/nimdaz/open-workout-app:latest
    ports:
      - "5000:5000"
    volumes:
      - ./data:/code/data
    restart: unless-stopped
